#Test React App

Built with:

`React Redux React-Router Material UI`

REST API:

`node.js express`

To run dev:

`npm i`

`nodemon app.js` and `npm start` in different terminals

To run production: 

`npm run build`
